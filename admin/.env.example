# Admin Configuration
VITE_API_URL=http://localhost:8787
# ⚠️ VITE_API_SECRET_KEY is baked into the JS bundle at build time.
# Anyone with browser dev tools can see it. For production, prefer
# setting the API key via the Admin Config page (localStorage).
VITE_API_SECRET_KEY=your_api_secret_key_here
